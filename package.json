{
  "name": "react-sse-chat-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @devscalelabs/react-sse-chat build",
    "dev": "pnpm --filter @devscalelabs/react-sse-chat dev",
    "typecheck": "pnpm -r typecheck"
  },

  "engines": {
    "node": ">=18.0.0"
  }
}
